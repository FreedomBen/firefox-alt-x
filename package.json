{
  "name": "firefox-ctrl-x-extension",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "start": "web-ext run --source-dir=.",
    "lint": "web-ext lint --source-dir=.",
    "build": "web-ext build --source-dir=. --artifacts-dir=build"
  },
  "devDependencies": {
    "web-ext": "^8.4.0"
  }
}
